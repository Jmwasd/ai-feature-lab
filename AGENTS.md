# ai-feature-lab

이 저장소는 서로 독립적인 실험 프로젝트를 한 폴더에 모아둔 것이다. 각 프로젝트는 자체 스택·규칙·문서를 가지며 서로 공유하는 코드나 설계는 없다.

## 프로젝트 격리 규칙

- CRITICAL: **작업 중인 프로젝트 폴더 안의 파일만 참고하라.** 다른 프로젝트 폴더(`whiteboard-editor/`, `harness/`, `peerlens/` 등)의 코드·문서·설정을 읽거나, 그 규칙과 패턴을 현재 작업에 끌어오지 마라.
- 예외: **사용자가 특정 프로젝트를 명시적으로 지시한 경우에만** 그 프로젝트를 참고한다. "harness 구조를 참고해서", "peerlens의 CLAUDE.md처럼" 같은 지시가 있을 때만 해당 범위를 연다.
- 사용자 지시로 예외가 열려도, 지시된 프로젝트로만 범위를 한정한다. 거기서 다시 제3의 프로젝트로 확장하지 마라.
- 참고 대상이 애매하면 추측해서 다른 프로젝트를 열지 말고 사용자에게 물어라.

**Why:** 프로젝트마다 스택과 제약이 다르다(예: Vite 기반과 Next.js 기반이 공존한다). 다른 프로젝트의 패턴을 가져오면 그 프로젝트에서만 유효한 결정이 근거 없이 전파되어 일관성이 깨진다.

**How to apply:** 작업 시작 시 대상 프로젝트 폴더를 확정하고, 검색·탐색 범위를 그 폴더로 제한한다. 프로젝트별 지침은 해당 폴더의 `CLAUDE.md`를 따른다. 저장소 루트의 공통 파일(`README.md`, `.claude/`)은 항상 참고 가능하다.

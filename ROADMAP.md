# 로드맵

학습이 목적이므로 각 단계에 **무엇을 배우는가**를 함께 적는다.
순서는 의존성 순이다. M3(성능)을 M5(협업)보다 먼저 두는 이유는, 협업이 붙은 뒤에 성능 문제를 만나면 원인이 렌더링인지 네트워크인지 구분하기 어려워지기 때문이다.

---

## M0 — 기반 ✅ 완료

렌더 루프, 좌표계, 커맨드 히스토리, 사각형·타원 그리기, 선택·이동·삭제, 팬·줌.

**배운 것**: 명령형 렌더 루프와 React의 분리, 스크린/월드 좌표 변환, 커서 기준 줌 보정, Command 패턴, devicePixelRatio 대응.

---

## M1 — 도형 조작 ✅ 완료

- [x] 리사이즈 핸들 8방향 (모서리 + 변)
- [x] `Shift` 드래그로 종횡비 고정, `Alt`로 중심 기준 리사이즈
- [x] 마퀴 선택 (빈 곳 드래그로 사각 영역 선택)
- [x] `Shift` 클릭으로 선택 추가·제거
- [x] 회전 (`Shift`로 15° 스냅)
- [x] 회전각을 반영한 방향별 리사이즈 커서
- [x] **다중 선택 시 공통 바운딩 박스로 함께 리사이즈** — 회전이 섞이면 균등 스케일로 제한

**결정**: 회전은 변환 행렬 대신 `rotation` 각도 하나로 표현한다. skew나 비균등 변환이 필요 없고, 히트 테스트를 "역회전 후 AABB 검사"로 유지할 수 있기 때문이다. 행렬이 필요해지는 시점(중첩 그룹 변환 등)에 승격한다.

**다중 선택 리사이즈 — 미뤄뒀던 것을 결국 행렬 없이 끝냈다.** 걸림돌은 그대로였다. 공통 박스의 x·y를 다른 비율로 늘이면 그 축에 비스듬히 놓인 사각형은 평행사변형이 되어야 하는데 `rotation` 하나로는 표현할 수 없다. 계획해둔 타협안은 "회전이 섞이면 균등 스케일만"이었는데, 실제로 짜보니 **경계가 '회전이 섞였는가'가 아니라 '90°의 배수인가'였다.** 90°의 배수면 폭과 높이가 맞바뀔 뿐 사각형이 사각형으로 남으므로 비균등 스케일이 정확히 표현된다. 배수가 아닌 도형이 하나라도 있을 때만 종횡비를 강제로 고정한다.

**배운 것**

- **핸들 히트 영역은 `크기 / zoom`으로 화면 기준을 유지한다.** 앵커 고정 리사이즈는 로컬 좌표계 왕복으로 일반화되고, 회전된 도형에서 중심 보정을 빠뜨리면 도형이 미끄러진다.
- **리사이즈의 대상이 도형이 아니라 상자라는 것을 알아차리자 단일 선택 코드가 사라졌다.** `Box`(사각형 + 회전각)를 도입하고 "도형은 상자 안에서의 상대 위치를 유지한 채 같은 비율로 커진다"로 다시 쓰니, 도형이 하나뿐인 경우는 상대 위치가 늘 한가운데인 특수 케이스가 됐다. 다중 선택을 **추가**한 게 아니라 기존 코드를 일반화한 결과로 얻었다. 회귀 검증도 여기서 나온다 — 옛 구현과 800개 조합을 비교해 부동소수 수준까지 같음을 확인했다.
- **박스를 렌더러와 입력 처리가 각자 계산하게 두면 안 된다.** 전에는 "핸들을 그릴까"(renderer)와 "핸들을 집을까"(CanvasView)가 각자 `selection.length === 1`을 검사했다. 조건이 하나뿐일 때는 버틸 수 있지만, 다중 선택이 붙으면서 박스 계산과 회전 핸들 노출 여부까지 양쪽이 알아야 했다. 보이는 핸들과 집히는 핸들이 어긋나는 건 원인을 찾기 어려운 종류의 버그라, 갈라지기 전에 `core/selection.ts`로 합쳤다.
- **표현할 수 없는 조작은 왜곡하지 말고 제약해야 한다.** 회전이 섞였을 때 억지로 비균등 스케일을 적용하면 도형이 슬금슬금 어긋나고, 그건 되돌리기로도 정확히 복구되지 않는다. 종횡비를 고정해 "덜 되지만 정확한" 결과를 주는 편이 낫다.
- **동작하지 않는 손잡이는 그리지 않는다.** 공통 박스에는 회전 핸들을 내지 않는다. 그리면 잡히고, 잡히면 되어야 한다.

**여전히 행렬이 필요한 것**: 회전이 섞인 다중 선택의 비균등 스케일, 그리고 중첩 그룹 변환. M6의 그룹화에서 다시 마주칠 문제다.

---

## M2 — 도형 종류 확장 ✅ 완료

- [x] **`shapes/` 레지스트리 도입 — 이 단계의 첫 작업**
- [x] 자유 곡선 (펜)
- [x] 텍스트 (생성·더블클릭 편집·리사이즈 시 글꼴 스케일)
- [x] 선/화살표
- [x] 색상·선 굵기 편집 패널
- [x] 필압(`PointerEvent.pressure`)과 `getCoalescedEvents()`로 획 품질 개선
- [x] 펜 히트 테스트 여유 폭을 줌에 맞춰 보정
- [x] 텍스트 정렬(좌/중앙/우)과 고정 폭 자동 줄바꿈
- [x] 텍스트 편집 중 IME 조합 상태 처리 검증 (한글·일본어 입력)

**레지스트리를 먼저 한 결과**: 도형 타입 분기가 `geometry.ts`·`transform.ts`·`renderer.ts` 세 곳에 흩어져 있던 것을 `shapes/`로 모았다. 그 뒤 펜을 추가할 때 **기존 파일에서 고친 것은 도구 등록(ToolId, 툴바 버튼, 단축키)뿐이고, 리사이즈·회전·핸들 코드는 한 줄도 건드리지 않았다.**

예상보다 레지스트리가 작아졌다. 처음에는 `hitTest`/`getBounds`/`resize`/`draw` 넷을 도형별로 둘 생각이었지만, 펜의 점을 바운딩 박스 기준으로 정규화하자 `getBounds`와 `resize`가 모든 도형에서 같아졌다. 결국 도형별로 다른 것은 **히트 테스트와 그리기 둘뿐**이다.

**단계가 끝난 뒤 레지스트리가 맡는 일은 넷이 됐다.** 히트 테스트, 그리기, `styleFields`(패널이 무엇을 보여줄지), `adjustAfterResize`(공통 리사이즈 뒤처리). 셋째와 넷째는 도형이 늘면서 생긴 것이지, 처음부터 예상한 것이 아니다.

**배운 것**

- **선은 박스 모델에 안 맞는 첫 도형이었지만 펜과 같은 방법으로 흡수됐다.** 양 끝점을 박스 기준 0~1로 정규화하니 어느 대각선인지가 좌표에 그대로 남아 리사이즈·회전 코드를 한 줄도 고치지 않았다. 대가는 수평선·수직선이 한 축이 0인 퇴화 박스가 된다는 것이다. 끝점 핸들을 주고 싶어지는 순간 — 핸들 구성이 도형마다 달라지는 순간 — 이 모델은 한계에 부딪힌다.
- **필압은 점에 실어 보내면 저절로 따라온다.** RDP 단순화가 점을 새로 만들지 않고 원본 원소를 그대로 통과시키기 때문에, `simplifyPath`를 제네릭으로 바꾼 것 말고는 손댈 곳이 없었다. 자료구조를 잘 고르면 기능 하나가 코드 한 줄로 끝난다는 사례.
- **가변 폭 획은 경로 하나로 그릴 수 없다.** `lineWidth`는 경로마다 하나뿐이라, 점마다 짧은 곡선 조각으로 끊어 각자의 굵기로 긋고 둥근 캡으로 이음매를 메운다. 필압이 없는 획은 예전처럼 경로 하나로 긋는다 — 훨씬 싸기 때문이다.
- **`getCoalescedEvents()`는 브라우저가 프레임 사이에 버린 위치를 되찾는다.** 포인터 이벤트는 화면 갱신 주기로 합쳐져 들어오므로, 빠르게 그으면 획이 눈에 띄게 각진다.
- **히트 판정의 여유 폭은 월드가 아니라 화면 기준이어야 한다.** 월드 단위로 고정하면 축소했을 때 얇은 획이 화면상 실오라기가 되어 집을 수 없다. 이 때문에 `hitTest` 체인 전체가 `zoom`을 받게 됐다 — 판정이 카메라를 알아야 하는 첫 사례다.
- **텍스트 줄바꿈은 측정만 주입받으면 순수 함수로 남는다.** 규칙(단어 단위로 채우다 단어 하나가 폭을 넘으면 글자 단위로 끊기 — 띄어쓰기 없는 한국어 문장 때문에 필요하다)은 `shapes/text.ts`에 두고, `ctx.measureText`는 밖에서 넣는다. 서버가 같은 줄바꿈을 재현해야 할 때 폰트 메트릭 테이블을 끼워 넣을 자리가 생겼다.
- **리사이즈의 의미가 핸들마다 갈렸다.** 텍스트는 좌우 핸들이 줄바꿈 폭을, 코너·상하 핸들이 글꼴 크기를 바꾼다. 그래서 `adjustAfterResize`가 "어느 핸들을 끌었는지"를 알아야 했다.
- **IME 조합 중의 `Enter`와 `Escape`는 IME의 것이다.** 한글에서 Enter는 조합 확정, Escape는 조합 취소다. `isComposing`을 확인하지 않으면 확정하려던 글자가 사라지거나 한 글자 지우려던 Escape가 편집 전체를 닫는다.
- **React의 `onChange`는 DOM의 `input`이다.** 색상 피커를 끄는 내내 들어오므로 그대로 히스토리에 쌓으면 되돌리기가 색 한 단계씩 되감긴다. 조작이 끝나는 시점인 DOM `change`는 React가 노출하지 않아 직접 붙였다. 미리보기는 씬에 바로, 히스토리에는 한 번만.
- **패널이 생기자 "도형마다 의미 있는 속성이 다르다"가 눈에 보이는 문제가 됐다.** 각 도형 모듈이 `styleFields`로 선언하고 패널은 그 합집합을 그린다. 도형을 추가해도 패널 코드는 바뀌지 않는다.

---

## M3 — 성능 🔧 진행 중

- [x] 도형 1만 개 생성 벤치마크 스크립트 (`npm run bench`)
- [x] 뷰포트 컬링 (화면 밖 도형 건너뛰기)
- [x] 공간 인덱스 도입 (quadtree)
- [x] 정적 레이어 / 활성 레이어 캔버스 분리
- [ ] 프레임 타임 계측 오버레이

**측정 먼저**: 최적화 전에 반드시 재고 기록해둔다. 추측으로 고치면 무엇이 효과가 있었는지 알 수 없다. 이 규약이 실제로 값을 했다 — **아래 표의 첫 줄이 이 단계에서 가장 중요한 결과다.**

### 측정 결과 (도형 1만 개 · 월드 6000² · 뷰포트 1440×900 · 60fps 예산 16.67ms)

| 항목 | 손대기 전 | 컬링 후 | 인덱스 후 | 레이어 후 |
| --- | --- | --- | --- | --- |
| 렌더 · 드래그 중 | 55.7ms | 2.3ms | 2.3ms | **0.2ms** |
| 렌더 · 배율 1 | 55.7ms | **2.3ms** | 2.3ms | 2.3ms |
| 렌더 · 팬 중 | 23.3ms | **2.4ms** | 2.4ms | 2.3ms |
| 렌더 · 전체 보기 | 124.2ms | 105.0ms | 105.0ms | 103.3ms |
| 마퀴 한 프레임 | 0.63ms | 0.63ms | **0.045ms** | 0.045ms |
| 픽 · 빈 곳 클릭 | 0.56ms | 0.56ms | **~0** | ~0 |
| 픽 · 편집 직후 | 0.5ms | 0.5ms | **3.0ms** ⚠︎ | 2.8ms |

마지막 열에서 **실제로 달라진 것은 첫 줄 하나뿐이다.** 나머지 항목은 레이어를 타지 않고 `renderScene`을 그대로 부르므로 숫자가 같아야 맞고, 그럼에도 ±10%쯤 흔들린다 — 전체 보기는 실행에 따라 103~114ms를 오간다. **한 번 재고 좋아졌다고 말하면 안 되는 폭이다.** 첫 줄이 11배인 것은 그 폭을 한참 넘어서라 믿을 수 있다.

**밀도를 고정하고 도형 수만 늘렸을 때** (마퀴 한 프레임):

| 도형 수 | 전체 훑기 | 공간 인덱스 | 차이 | 인덱스 재구축 |
| --- | --- | --- | --- | --- |
| 2,500 | 0.110ms | 0.035ms | 3.1× | 0.6ms |
| 10,000 | 0.575ms | 0.045ms | 12.8× | 2.8ms |
| 40,000 | 3.015ms | 0.050ms | 60.3× | 14.2ms |

**배운 것**

- **이 단계의 전제가 틀렸다는 것을 측정이 알려줬다.** 원래 여기 적혀 있던 말은 "`pickTopmost`가 O(n)이라 1만 개에서 포인터 이동만으로 프레임이 무너진다"였다. 실제로는 **픽이 예산의 3%(0.56ms)였고, 무너지는 것은 렌더(334%)였다.** 게다가 포인터를 움직일 때 `pickTopmost`는 아예 불리지 않는다 — 호버 강조 기능이 없어서 누를 때만 돈다. 추측대로 공간 인덱스부터 넣었다면 3%를 0%로 만드느라 334%를 방치했을 것이다.
- **컬링은 화면 밖이 있을 때만 효과가 있다.** 배율 1에서 24배가 빨라졌지만 전체 보기에서는 124ms → 105ms에 그친다. 다 보이면 다 그려야 하기 때문이다. 이 한계가 다음 항목(레이어 분리)과 언젠가의 LOD를 정당화한다.
- **공간 인덱스의 값어치는 한 시점의 숫자가 아니라 기울기에 있다.** 1만 개에서는 0.63ms → 0.045ms로 "있으면 좋은" 정도지만, 도형 수를 16배로 늘리면 차이가 3배에서 60배로 벌어진다. 전체 훑기는 n을 따라 늘고 인덱스는 거의 평평하다. **밀도를 함께 올리면 이 기울기가 안 보인다** — 결과 개수가 n에 비례해 늘어 양쪽 다 O(n)처럼 보인다. 무엇을 고정하고 무엇을 늘릴지가 측정 설계의 전부다.
- **빨라진 대신 새로 생긴 비용을 같은 표에 적어야 한다.** 인덱스는 씬이 바뀔 때마다 다시 세워지므로 편집 직후 첫 클릭이 0.5ms → 3.0ms로 **6배 느려졌다.** 4만 개에서는 14.2ms로 프레임 예산의 85%다. 질의를 공짜로 만든 대가이고, 이 방식의 천장이다.
- **컬링과 인덱스의 실패 방식은 느려지는 것이 아니라 조용히 틀리는 것이다.** 여유 폭을 조금 좁게 잡으면 얇은 선이 후보에서 빠지거나 화면 가장자리 도형이 사라지는데, 아무 오류도 나지 않는다. 그래서 벤치마크가 숫자를 잴 때마다 정합성을 함께 확인한다 — 인덱스는 전체 훑기와 결과를 대조하고(1,400회), 컬링은 **버린 도형만 모아 다시 그려서** 한 점이라도 칠해지는지 본다.
- **인덱스는 후보를 좁히기만 하고 판정은 여전히 `hitTest`가 한다.** 이 분업 덕분에 인덱스가 들고 있는 사각형은 실제 판정 영역보다 **넉넉하기만 하면 된다** — 모자라면 놓치지만 남으면 후보가 몇 개 늘 뿐이다. 이 비대칭이 여유 폭을 대충 크게 잡아도 되는 근거다.
- **검사가 실패를 잡는지 확인하지 않으면, 검사가 있다는 착각만 남는다.** 컬링 검증을 처음에는 "버린 도형만 좁은 화면에 다시 그려본다"로 짰다. 통과했다. 그런데 일부러 컬링을 30px 과하게 망가뜨려도 여전히 통과했다 — **다시 그릴 때 `renderScene`이 그 도형을 또 걸러냈기 때문이다.** 검사가 자기가 검사하려는 코드를 그대로 타고 있었다. 세 배 넓은 화면에 그려 걸러지지 않게 고치자 비로소 범인 넷을 집어냈다.
- **픽셀 비교에는 눈에 안 보이는 함정이 있다.** 그 전에는 "좁게 그린 화면"과 "넓게 그린 화면의 가운데"를 맞대 봤는데 0.9%가 어긋났다. 컬링을 끄고 양쪽에 **완전히 같은 도형 집합**을 그려도 똑같이 어긋났다 — 같은 도형이라도 캔버스 크기가 다르면 래스터라이저의 안티에일리어싱 커버리지가 미세하게 달라진다(최대 30/255, 텍스트만 0). 도형이 통째로 빠지면 배경색과의 차이라 100을 넘으니 임계값으로 가를 수도 있었지만, **임계값이 필요 없는 판정이 있으면 그쪽이 낫다.**
- **최적화가 숨어 있던 버그를 드러냈다.** `rect.draw`가 `lineJoin`을 지정하지 않아 직전에 그려진 도형이 남긴 값을 물려받고 있었다. 펜(`lineJoin = 'round'`) 바로 뒤에 그려지면 모서리가 둥글어진다. 컬링이 그리는 순서를 바꾸면서 눈에 띄었다 — **Canvas 상태는 전역이고, 그리는 순서가 바뀔 수 있게 된 순간부터 각 도형은 자기가 쓰는 상태를 전부 지정해야 한다.**
- **최적화가 병목을 옮겼을 뿐 없애지는 않았다.** 밀도를 고정했는데도 배율 1 렌더가 1.5ms → 2.2ms → 6.3ms로 늘어난다. 화면에 걸리는 도형 수는 같은데, **컬링 판정 자체가 전체 도형을 훑는 O(n)이기 때문이다.** 지금은 그리기 비용이 압도적이라 묻히지만, 다음 병목은 여기다. → 레이어 분리가 이걸 드래그 중에 한해 걷어냈다.

- **레이어 분리에서 어려운 것은 그리기가 아니라 "언제 낡았는가"다.** 캔버스를 둘로 나눠 각각 무엇을 그릴지 정하는 데는 30분이 걸리지 않았다. 나머지 전부는 **정적 레이어를 다시 그려야 할 때를 놓치지 않는 문제**였다. 캐시를 도입하는 일이 늘 그렇듯, 만드는 쪽이 아니라 버리는 쪽이 본체다.
- **씬 참조로는 판단할 수 없었고, 그 사실이 무효화 신호를 스토어로 밀어 올렸다.** 드래그 중에는 매 프레임 새 씬이 만들어지지만 실제로 바뀐 것은 잡고 있는 도형뿐이다. 씬이 달라졌다는 이유로 다시 그리면 레이어를 나눈 이득이 통째로 사라지고, 안 그리면 잔상이 남는다. **어떤 종류의 변경인지 아는 것은 스토어뿐이었다** — M0에서 만들어둔 `execute`/`setScene`/`record` 3분기가 그대로 답이 됐다. 성능 최적화가 히스토리 설계에 기대게 될 줄은 예상하지 못했다.
- **예외는 하나여야 기억할 수 있다.** "씬을 바꾸는 모든 경로가 정적 레이어를 무효화한다, 단 활성 집합이 선언된 동안의 `setScene`만 예외" — 텍스트 편집 중의 갱신은 사실 무효화할 필요가 없지만(편집 중인 도형은 이미 정적 레이어에서 빠져 있다) 일부러 예외로 두지 않았다. 타자 속도로 도는 경로라 전체 렌더 한 번이 붙어도 체감되지 않고, **규칙 두 개는 규칙 하나보다 훨씬 비싸다.**
- **활성 레이어가 z-order를 어긴다는 것은 버그가 아니라 값을 치른 것이다.** 뒤에 있던 도형을 끌면 드래그하는 동안 앞으로 나온다. 정확히 하려면 매 프레임 전체 순서를 다시 세워야 해서 분리한 이유가 사라진다. M1에서 배운 "표현할 수 없는 조작은 왜곡하지 말고 제약해야 한다"와는 반대 결론인데, 차이는 **되돌릴 수 있는가**다 — 회전 왜곡은 문서에 남지만 이 어긋남은 포인터를 떼는 순간 사라진다.
- **검증 대상을 좁히자 임계값이 필요 없어졌다.** 처음에는 두 캔버스를 합성해 통짜 렌더와 비교하려 했는데, 투명 캔버스를 거치며 알파가 두 번 반올림되어 1~2/255가 어긋난다. 그런데 **활성 레이어는 매 프레임 새로 그려지므로 애초에 낡을 수가 없다.** 조용히 틀리는 쪽은 캐시가 있는 정적 레이어뿐이고, 그쪽만 보면 같은 크기 캔버스끼리의 불투명 렌더 비교라 정확히 같아야 한다. 컬링 검증에서 얻은 "임계값이 필요 없는 판정이 있으면 그쪽이 낫다"가 두 번째로 값을 했다.
- **검사가 실패를 잡는지 또 확인했다.** `key.active` 비교를 지우니 '드래그 중'·'놓은 뒤' 단계가, `key.epoch` 비교를 지우니 '드래그 중 되돌리기' 단계가 잡혔다 — **단계마다 겨냥하는 실패가 다르다는 것을 확인하고서야** 검사가 있다고 말할 수 있었다.

**다음**: 남은 O(n)은 팬·줌이다. 카메라가 움직이면 정적 레이어를 통째로 다시 그리므로 전체 훑기가 그대로 돌아온다. 후보는 둘 — 컬링에 공간 인덱스를 쓰거나(질의 결과를 z-order로 다시 세워야 한다), 정적 레이어를 어긋난 만큼 밀어 붙이고 새로 드러난 띠만 그리거나. 다만 팬 중 2.3ms는 예산의 14%다. **근거가 생기기 전에는 건드리지 않는다.**

---

## M4 — 영속성

- [ ] 씬 직렬화 / 역직렬화 + 스키마 버전 필드
- [ ] IndexedDB 자동 저장 (디바운스)
- [ ] 문서 여러 개 관리
- [ ] PNG / SVG 내보내기

**배우는 것**: 스키마 마이그레이션. M1에서 회전을, M2에서 펜을 추가하면 이전에 저장한 문서를 못 읽게 된다. 버전 필드와 업그레이드 함수를 처음부터 두면 이 문제가 사라진다.

---

## M5 — 실시간 협업

- [ ] Yjs 도입, 씬을 `Y.Map` 기반으로 전환
- [ ] Hono 기반 WebSocket 서버 (`server/`)
- [ ] 참여자 커서·선택 영역 표시 (awareness)
- [ ] 오프라인 편집 후 재접속 시 병합
- [ ] 서버 측 스냅샷 저장

**배우는 것**: CRDT가 왜 필요한지를 몸으로 이해하는 단계. 두 사람이 같은 도형을 동시에 옮기면 어떻게 되는가, 마지막 쓰기가 이기는(LWW) 방식으로 충분한 속성은 무엇이고 아닌 것은 무엇인가.

**여기서 지금까지의 구조가 시험대에 오른다**: undo가 협업에서는 "내가 한 것만 되돌리기"여야 한다. 전역 히스토리 스택은 이 시점에 다시 설계해야 하고, 그래서 M0에서 커맨드를 스냅샷이 아니라 역연산으로 만들어 둔 것이다.

**주의**: Yjs를 붙이는 순간 `Scene` 타입을 직접 다루던 코드가 전부 영향을 받는다. M4까지 끝내고 구조가 안정된 뒤에 착수한다.

---

## M6 — 마감

- [ ] 복사 / 붙여넣기 / 복제
- [ ] 그룹화
- [ ] 정렬·분배
- [ ] z-order 조작 (맨 앞/뒤로)
- [ ] 이미지 붙여넣기
- [ ] 접근성 — 키보드만으로 도형 선택·이동

---

## 디렉터리 구조 진화

폴더는 늘어난다. 다만 꾸준히가 아니라 **M2·M3·M5 세 번의 전환점에서 계단식으로** 늘고, M6은 구조 변화가 거의 없다. M2를 끝낸 지금 5폴더 22파일이고, 최종 12폴더 45~50파일 정도가 된다.

### M0~M1

```
src/
├─ core/    camera, geometry, transform, commands, history, types
├─ render/  renderer
├─ store/   editorStore
└─ ui/      CanvasView, Toolbar
```

### M2 — `shapes/` 등장. 가장 큰 전환점

```
src/
├─ core/     좌표·히스토리 등 도형 종류와 무관한 것만 남는다
├─ shapes/   ← 신규
│  ├─ registry.ts   ShapeType → 모듈 매핑 (순수)
│  ├─ render.ts     ShapeType → draw 매핑 (Canvas 의존)
│  ├─ rect.ts
│  ├─ ellipse.ts
│  ├─ path.ts       펜
│  ├─ line.ts       선·화살표
│  └─ text.ts
├─ render/   renderer는 순회와 변환만, 도형 그리기는 shapes/로 이관
├─ store/
└─ ui/       + PropertyPanel, TextEditor
```

실제로는 레지스트리가 **두 파일**로 갈라졌다. 판정은 서버가 그대로 쓸 수 있어야 하는데 그리기는 Canvas에 묶여 있어서, 한 파일에 두면 서버가 판정만 골라 쓸 수 없다.

모듈 형태는 이렇게 잡는다.

```ts
export const rectShape = {
  hitTest,    // 순수
  getBounds,  // 순수
  resize,     // 순수
  draw,       // 유일하게 브라우저(Canvas)에 의존
}
```

`geometry.ts`의 `hitTest`/`getWorldBounds`, `transform.ts`의 `resizeShape`, `renderer.ts`의 `drawShape`가 여기로 흡수되고, 세 파일에 흩어져 있던 타입 분기가 사라진다.

### M3 — 성능 인프라

```
src/core/spatial/   quadtree + sceneIndex — 픽·마퀴의 O(n)을 걷어낸다
src/dev/            ← 신규. 벤치마크 시드·하네스 (개발 빌드에만 실린다)
scripts/bench.mjs   ← 예측에 없던 것. 브라우저를 띄워 재는 CLI
src/render/layers.ts   정적/활성 레이어와 정적 레이어 캐시 무효화
```

**예측에서 빗나간 것**: `src/dev/`에 "프레임 타임 오버레이"를 두려 했지만 먼저 필요한 것은 **재현 가능한 CLI**였다. 오버레이는 지금 화면이 몇 fps인지 보여주고, CLI는 어제와 오늘을 비교해준다. 최적화에는 후자가 있어야 한다 — `--save`로 남기고 `--diff`로 견준다.

**`layers.ts`도 예측과 달랐다.** 파일 하나가 늘어난 것까지는 맞는데, 함께 `editorStore`에 필드 둘(`active`·`staticEpoch`)이 붙었다. 캐시를 언제 버릴지 판단할 정보가 `render/`에 없었기 때문이다 — **폴더 예측은 새로 생길 파일은 맞혀도 기존 파일에 생길 구멍은 못 맞힌다.**

### M4 — 영속성

```
src/persistence/
├─ serialize.ts
├─ storage.ts        IndexedDB
└─ migrations/       ← 신규. v1→v2 등 스키마 업그레이드
```

### M5 — 최상위 구조가 바뀐다

```
lab-ai/
├─ src/
│  └─ collab/       ← 신규. Yjs 바인딩, awareness
├─ server/          ← 신규. Hono + Yjs WebSocket + 스냅샷
├─ shared/          ← 신규. 양쪽이 쓰는 타입·검증
├─ tsconfig.app.json
└─ tsconfig.server.json
```

**workspaces 모노레포로 가지 않는다.** 서버가 하나뿐인데 패키지 경계를 강제하면 빌드 설정과 의존성 관리 비용만 늘어난다. 폴더와 tsconfig 분리로 충분하고, 필요해지면 그때 승격한다.

### M6

복사/붙여넣기, 내보내기, 정렬은 기존 폴더에 흡수된다.

### 규약

1. **폴더를 미리 만들지 않는다.** 위 구조는 예측이지 착수 목록이 아니다. `shapes/`도 도형이 2종뿐이던 M1까지는 만들지 않았다 — 그때 만들었다면 레지스트리를 유지하는 비용만 생겼을 것이다.
2. **`core/`는 브라우저 API를 모른다.** 브라우저 의존은 `shapes/`(draw), `render/`, `ui/`에만 허용한다.
3. **`shapes/` 모듈에서 `draw`는 별도 export로 분리해 둔다.** 순수 함수와 한 파일에 있되 섞이지 않아야 서버가 `hitTest`/`getBounds`만 골라 쓸 수 있다.
4. **판단 기준은 폴더 개수가 아니라 "한 가지를 바꿀 때 열어야 하는 파일 수"다.** 도형 추가 시 이 값이 1(+registry 등록)로 유지되면 구조가 건강한 것이다.

---

## 지금 당장 다음에 할 것

**M3의 마지막 항목, 프레임 타임 계측 오버레이.** CLI는 어제와 오늘을 비교해주지만 "지금 이 화면에서 왜 끊기는가"에는 답하지 못한다. 레이어를 나눈 뒤로는 그 질문이 더 구체적이 됐다 — **한 프레임에서 정적 레이어를 다시 그렸는가, 활성 레이어만 그렸는가**가 비용을 가르므로, 오버레이가 보여줘야 할 것은 fps 숫자보다 그쪽이다.

**전체 보기의 103ms는 레이어 분리로 안 풀린다.** 처음 한 번은 어차피 다 그려야 한다. 그건 LOD(축소 시 획을 단순화하거나 박스로 대체)의 영역이고, 아직 필요하다는 근거가 없다 — 측정해서 근거가 생기면 그때 한다.

**팬 중의 2.3ms도 마찬가지다.** 카메라가 움직이면 정적 레이어가 매 프레임 다시 그려져 전체 훑기가 돌아오지만, 예산의 14%라 아직 아프지 않다. 고치는 방법은 알고 있고(레이어를 밀어 붙이고 새로 드러난 띠만 그리기) 근거가 생기면 그때 한다.

**행렬 모델 승격은 계속 미룬다.** M1에 남아 있던 다중 선택 리사이즈는 "90°의 배수가 아니면 균등 스케일"이라는 제약으로 행렬 없이 끝냈다. 선을 박스 모델에 흡수시킨 것에 이어 승격 시점을 또 한 번 미룬 셈이다. 다음 후보는 M6의 그룹화다 — 중첩 변환은 제약으로 피해 갈 수 없다.

---
description: chat-logs 별칭 — 프로젝트 대화 기록을 질문·답변 HTML 한 장으로 묶는다
argument-hint: [프로젝트명]
---

`chat-logs` 스킬을 Skill 도구로 호출하고, `args` 에 `$ARGUMENTS` 를 그대로 넘겨라.

이 파일은 `/chat-logs` 의 짧은 진입점일 뿐이다. 실제 절차와 규칙은 전부 그 스킬에 있으니, 스킬을 부르지 않고 여기서 직접 처리하지 마라.

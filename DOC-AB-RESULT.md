# 폴더별 CLAUDE.md는 값을 하는가 — A/B 측정 (n=1)

측정일 2026-08-04 · 모델 claude-opus-5 · 조건당 1회

## 요약

같은 기능을 두 번 구현시켰다. 한 번은 `src/*/CLAUDE.md` 6개가 있는 상태로, 한 번은 없는 상태로.

**결과물의 품질은 같았고(6/6 대 6/6), 비용은 37% 더 들었다.**

다만 이 한 번의 측정으로 "폴더 문서가 손해"라고 결론지을 수 없다. 채점이 천장에 붙어 변별력을 잃었고, 무엇보다 **핵심 함정의 근거가 폴더 문서가 아니라 양쪽 조건에 모두 있는 `ARCHITECTURE.md`에 있었다.** 설계 결함이다 — 아래 "이 측정이 답하지 못한 것"에 적는다.

## 무엇을 쟀나

문서는 공짜가 아니다. 읽는 순간 input 토큰을 먹는다. 그 대가로 탐색이 줄고 함정을 덜 밟아야 본전이다. 그래서 물은 것은 하나다.

> 문서를 싣는 토큰 비용 < 탐색 절감 + 재작업 방지 + 품질 향상인가?

재려는 대상은 "문서 전체"가 아니라 **폴더별로 쪼개 둔 구조**다. 루트 `CLAUDE.md`와 `ARCHITECTURE.md`는 양쪽에 그대로 뒀다.

| 조건 | 내용 |
| --- | --- |
| `exp/docs-on` | 현재 상태 그대로 |
| `exp/docs-off` | `src/*/CLAUDE.md` 6개 삭제 + 루트의 참조 표 + `ARCHITECTURE.md`의 포인터 7줄 제거 |

포인터까지 지운 것은, 남겨두면 없는 파일을 가리켜 헛읽기가 생기기 때문이다. 그러면 "문서 없음"이 아니라 "깨진 문서"를 재게 된다. `ARCHITECTURE.md`의 **본문은 한 글자도 바꾸지 않았다.**

### 태스크

양쪽에 글자까지 같은 프롬프트를 줬다.

```
도형에 불투명도(opacity) 스타일을 추가해줘. 속성 패널에서 0~1 사이 값을
조절할 수 있고, 모든 도형 종류에 적용된다. 기본값은 1이다.
```

일부러 짧고 열려 있다. 상세히 지시하면 지시문이 문서를 대신해버려 재려던 것이 사라진다.

이 태스크를 고른 이유는 **타입체커도 벤치마크도 잡지 못하는 함정**이 있어서다. `globalAlpha`는 Canvas 전역 상태라, 일부 도형만 지정하면 지정하지 않은 도형이 앞 도형의 알파를 물려받아 **그리는 순서에 따라** 엉뚱하게 반투명해진다. 빌드도 벤치도 통과하고 특정 순서에서만 보인다 — `rect.draw`가 `lineJoin`을 빠뜨렸던 사건과 같은 형태다.

## 결과

| | docs-on | docs-off | 차이 |
| --- | ---: | ---: | ---: |
| 캐시 생성 토큰 | 152,743 | 133,494 | **+19,249 (+14%)** |
| 캐시 읽기 토큰 | 5,809,854 | 3,664,991 | **+2,144,863 (+59%)** |
| output 토큰 | 37,450 | 32,718 | +14% |
| input 토큰 | 81 | 957 | — |
| 턴 수 | 68 | 68 | **0** |
| 전체 도구 호출 | 67 | 67 | **0** |
| 탐색 호출 (Read·Grep) | 18 | 22 | **-4 (-18%)** |
| 소요 시간 | 568초 | 472초 | **+20%** |
| 비용 | **$5.37** | **$3.93** | **+37%** |

| 검증 | docs-on | docs-off |
| --- | --- | --- |
| 체크리스트 | **6/6** | **6/6** |
| `npm run build` | ✓ | ✓ |
| `npm run bench` 정합성 | ✓ | ✓ |
| 알파가 새는 위험 상태 | 없음 | 없음 |

**캐시 생성 토큰 차이 19,249개가 폴더 문서 6개(49KB)의 크기와 대체로 맞아떨어진다.** 실험이 의도한 것을 실제로 재고 있었다는 정합성 신호다.

읽는 값은 하나뿐이다. **문서를 실은 쪽이 탐색을 4회 덜 했고(18 대 22), 그 대가로 매 턴 무거운 컨텍스트를 끌고 다녀 캐시 읽기가 59% 늘었다.** 턴 수와 도구 호출 총량은 68과 67로 완전히 같았다.

## 채점이 한 번 틀렸다

처음 자동 채점은 6/6 대 **5/6**을 냈다. docs-off가 "도형을 그린 뒤 알파를 되돌림"에서 실패로 찍혔다.

확인해보니 오탐이었다. 채점이 `globalAlpha = 1` 패턴만 찾았는데, docs-off는 이렇게 썼다.

```ts
const prevAlpha = ctx.globalAlpha
ctx.globalAlpha = shape.opacity
// …
ctx.globalAlpha = prevAlpha
```

**이쪽이 오히려 정확하다.** 호출부가 이미 알파를 걸어둔 경우까지 안전하기 때문이다. 더 나은 구현을 실패로 처리하고 있었다.

채점을 고쳐(`= 1` / 이전 값 복원 / `save`-`restore` 셋 다 인정) 재채점한 결과가 위 표의 6/6 대 6/6이다. 고친 뒤 복원이 아예 없는 가짜 코드로 다시 검사해 여전히 실패하는 것도 확인했다 — 통과만 하는 검사는 검사가 아니다.

기록해 두는 이유는, **자동 채점이 실험의 결론을 한 번 뒤집었기 때문이다.** 정정하지 않았다면 "문서가 품질을 높인다"는 결론이 나왔을 것이다.

## 코드에서 관찰된 것

**둘 다 같은 결론에 다른 자리로 도달했다.** 도형 다섯에 나눠 맡기지 않고 공통 호출부에서 한 번에 거는 쪽을 골랐는데, 그 자리가 달랐다.

| | 자리 | 방식 |
| --- | --- | --- |
| docs-on | `render/renderer.ts`의 `drawShape` | `opacity < 1`일 때만 걸고 `= 1`로 복원 |
| docs-off | `shapes/render.ts`의 `drawShapeBody` | 이전 값을 저장했다 복원 |

둘 다 순서 의존성을 없앴고, 둘 다 이 선택의 근거를 코드 주석과 문서에 남겼다.

**양쪽 모두 문서를 함께 고쳤다.** docs-on은 `ARCHITECTURE.md`·`README.md`에 더해 `shapes/CLAUDE.md`와 `render/CLAUDE.md`까지, docs-off는 폴더 문서가 없으니 `ARCHITECTURE.md`와 `README.md`에 적었다. 시키지 않았는데 양쪽 다 했다.

**양쪽 모두 `dev/seed.ts`까지 고쳤다.** 벤치 씬에 반투명 도형을 섞지 않으면 알파 관련 실수가 정합성 검사에 걸리지 않는다는 것을 알아챈 것이다. docs-off는 여기에 "0.4 아래로는 내리지 않는다 — 너무 옅으면 진짜 컬링 버그가 배경과 구분되지 않아 검사가 통과해 버린다"는 근거까지 달았다. `dev/CLAUDE.md` 없이 도달한 판단이다.

**양쪽 다 `npm run bench`를 돌리려다 막혔다.** headless에서는 권한을 물을 수 없어 거부됐다(docs-on 4회, docs-off 7회 거부). 정합성 검사를 스스로 확인하지 못한 채 끝났다는 뜻이다. 같은 규칙이 양쪽에 적용됐으므로 비교 자체는 공평하지만, 다음 실행에서는 `--allowedTools`에 `Bash(npm run bench)`를 넣어야 한다.

## 해석

측정 전에 정해 둔 기준표에 대입하면 **"토큰↑ + 점수 동일 → 문서가 순손실"** 칸이다.

하지만 그렇게 읽으면 안 된다. 이유가 둘이다.

**1. 채점이 천장에 붙었다.** 양쪽 다 만점이라 변별력이 없다. 두 결과물의 실제 차이(구현 위치, 주석의 깊이, 문서 갱신 범위)는 체크리스트가 잡지 못한다. 더 어려운 태스크나 더 촘촘한 채점이 필요하다.

**2. 핵심 함정의 근거가 양쪽 조건에 다 있었다.** 이것이 더 큰 문제다. `globalAlpha` 함정의 근거로 `shapes/CLAUDE.md`를 골랐지만, 같은 내용이 `ARCHITECTURE.md` **불변식 7**에도 있다.

> **Canvas 상태는 전역이라 각 `draw`는 자기가 쓰는 상태를 전부 지정해야 한다.** `rect.draw`가 `lineJoin`을 빠뜨려 펜 뒤에 그려질 때만 모서리가 둥글어진 버그가 실제로 있었다.

`ARCHITECTURE.md`는 양쪽 조건에 그대로 있다. 실제로 docs-off는 주석에 "불변식 7 — 각 draw는 자기가 쓰는 상태를 전부 지정한다"고 인용하며 구현했다. **채점 항목이 조건을 가르지 못하도록 설계돼 있었다.**

그래서 이 측정이 실제로 보여준 것은 이것이다.

> 폴더 문서가 담은 규범이 `ARCHITECTURE.md`에도 있으면, 폴더 문서를 지워도 결과가 같고 비용만 37% 더 든다.

이건 폴더 문서 무용론이 아니라 **두 문서 사이의 중복을 가리키는 신호다.** 폴더 문서의 값어치는 "루트 문서에 없는 것을 담을 때" 나온다.

## 이 측정이 답하지 못한 것

- **n=1이다.** 모델 출력의 분산이 커서 한 번의 차이는 문서 효과인지 그날의 운인지 구분되지 않는다. 시간(568 대 472초)과 비용 차이도 마찬가지다.
- **`ARCHITECTURE.md`에 없는 규범은 재지 못했다.** 폴더 문서에만 있는 것 — `ui/CLAUDE.md`의 IME 처리, `store/CLAUDE.md`의 `commitTextEditing` 네 갈래, `dev/CLAUDE.md`의 검사 함정 — 을 겨냥한 태스크여야 폴더 문서 고유의 값어치가 드러난다.
- **에이전트가 정합성 검사를 돌리지 못했다.** 스스로 검증할 수 있었다면 결과가 달랐을 수 있다.

## 다음에 잰다면

1. **채점 항목을 `ARCHITECTURE.md`에 없는 것으로만 고른다.** 지금 설계로는 무엇을 재도 같은 결과가 나온다.
2. **`--allowedTools`에 `Bash(npm run bench)`를 넣는다.**
3. **조건당 최소 3회.** 지금 숫자는 방향일 뿐 크기가 아니다.
4. 천장을 피하려면 태스크를 더 어렵게 — 텍스트 편집 확정 경로나 레이어 무효화를 건드리는 것이 후보다.

## 재현

```bash
node scripts/doc-ab.mjs --runs 1          # 조건당 1회
node scripts/doc-ab.mjs --report          # 저장된 결과만 다시 표로
node scripts/doc-ab-score.mjs <디렉토리>   # 채점만
```

| 산출물 | 위치 |
| --- | --- |
| 시행별 raw 데이터 | `<out>/results.json` |
| 세션 로그 (stream-json) | `<out>/logs/<조건>-<회차>.jsonl` |
| 실제 diff | `<out>/patches/<조건>-<회차>.patch` |

`<out>` 기본값은 scratchpad 아래이고 `--out`으로 바꾼다. 브랜치는 `exp/docs-on`·`exp/docs-off`이며, 시행마다 worktree를 새로 만들어 이전 결과가 다음 시행의 출발점이 되지 않게 한다.
